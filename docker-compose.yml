version: '3'

services:
  backend:
    build: .
    container_name: realchar-backend
    ports:
      - "8000:8000"
    env_file:
      - ./.env
  web:
    build: ./client/web/
    container_name: realchar-web
    ports:
      - "80:80"
    depends_on:
      - backend
    env_file:
      - ./.env